// Package must be named main so that Go knows where our application starts
package main

import "fmt"

//  function must be named main so that Go knows which code to execute when the application starts
func main() {
	fmt.Print("Hello World")
}
